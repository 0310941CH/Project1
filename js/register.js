var passwordInput = document.getElementById("password"); // The password input field
var lowercase = /[a-z]/g; // All lowercase letters
var uppercase = /[A-Z]/g; // All uppercase letters
var numbers = /[0-9]/g; // All numbers

passwordInput.onkeyup = function () {

}